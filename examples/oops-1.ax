// Data structures example (without classes)
// Simulating a Point with separate variables

let point_x = 10;
let point_y = 20;

// Calculate sum of coordinates
let sum = point_x + point_y;

// Conditional based on position
if (point_x > 0) {
    let quadrant = 1;
} else {
    let quadrant = 2;
}
