; Fibonacci sequence calculator

(let x (fn fibonacci (n)
  (if (<= n 1)
    n
    (+ (fibonacci (- n 1))
       (fibonacci (- n 2)))))
)
(print "Fibonacci sequence:")
(let i 0)
(while (< i 10)
  (print "fib(" i ") =" (x i))
  (let i (+ i 1)))

